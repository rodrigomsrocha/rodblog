query Posts {
  posts(orderBy: publishedAt_DESC) {
    id
    slug
    title
    excerpt
    publishedAt
    tags
    author {
      name
      picture {
        url
      }
    }
    coverImage {
      url
    }
  }
}
